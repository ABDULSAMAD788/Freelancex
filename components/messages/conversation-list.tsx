import { Input } from "@/components/ui/input"
export function ConversationList() { return <aside className="border-b border-border p-4 lg:border-b-0 lg:border-r"><Input aria-label="Search conversations" placeholder="Search conversations" /><div className="mt-4 space-y-2">{["Maya Chen", "Ravi Patel", "Northstar Labs"].map((name, index) => <button className={`w-full rounded-lg p-3 text-left ${index === 0 ? "bg-muted" : "hover:bg-muted/60"}`} key={name} type="button"><p className="text-sm font-medium">{name}</p><p className="mt-1 truncate text-xs text-muted-foreground">Latest project message</p></button>)}</div></aside> }