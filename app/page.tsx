import Link from "next/link"
import { ArrowRight, CheckCircle2, Search, ShieldCheck, Sparkles, Users } from "lucide-react"
import { Button } from "@/components/ui/button"
import { Card, CardContent } from "@/components/ui/card"
import { PublicShell } from "@/components/layout/public-shell"
import { categories, jobs } from "@/lib/mock-data"

export default function Home() {
  return <PublicShell><main><section className="relative overflow-hidden border-b border-border/70"><div className="mx-auto grid max-w-7xl gap-14 px-5 py-20 sm:px-8 lg:grid-cols-[1.1fr_.9fr] lg:items-center lg:py-28"><div><p className="mb-5 inline-flex items-center gap-2 rounded-full border border-primary/30 bg-primary/10 px-3 py-1.5 text-xs font-medium text-primary"><Sparkles className="size-3.5" />A better place for good work</p><h1 className="max-w-3xl text-5xl font-semibold leading-[1.05] tracking-tight sm:text-7xl">Find the people who make <span className="text-primary">good work</span> happen.</h1><p className="mt-7 max-w-xl text-base leading-7 text-muted-foreground sm:text-lg">FreelanceX connects ambitious teams with independent talent who bring craft, clarity, and momentum to every project.</p><div className="mt-9 flex flex-wrap gap-3"><Button asChild size="lg"><Link href="/jobs">Find freelance work <ArrowRight /></Link></Button><Button asChild size="lg" variant="outline"><Link href="/dashboard/client/jobs/new">Hire a freelancer</Link></Button></div></div><div className="rounded-2xl border border-border bg-card p-4 shadow-2xl shadow-primary/5"><div className="rounded-xl bg-background p-5"><div className="flex items-center justify-between"><div><p className="text-xs uppercase tracking-widest text-muted-foreground">Browse opportunities</p><p className="mt-2 text-lg font-medium">What are you looking for?</p></div><Search className="size-5 text-primary" /></div><div className="mt-5 flex items-center gap-2 rounded-lg border border-border px-3 py-3 text-sm text-muted-foreground"><Search className="size-4" />Product design, engineering, copy...</div><div className="mt-5 space-y-3">{jobs.slice(0, 3).map((job) => <Link className="block rounded-lg border border-border/70 p-3 transition-colors hover:border-primary/50" href={`/jobs/${job.id}`} key={job.id}><div className="flex items-center justify-between gap-3"><p className="text-sm font-medium">{job.title}</p><ArrowRight className="size-4 shrink-0 text-primary" /></div><p className="mt-2 text-xs text-muted-foreground">{job.category} · {job.budget}</p></Link>)}</div></div></div></div></section><section className="mx-auto max-w-7xl px-5 py-20 sm:px-8"><div className="flex items-end justify-between"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-primary">Explore the marketplace</p><h2 className="mt-3 text-3xl font-semibold tracking-tight">Start with what you need.</h2></div><Link className="hidden text-sm text-muted-foreground hover:text-foreground sm:block" href="/categories">View all categories <ArrowRight className="ml-1 inline size-4" /></Link></div><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{categories.slice(0, 4).map((category) => { const Icon = category.icon; return <Link href={`/jobs?category=${encodeURIComponent(category.name)}`} key={category.name}><Card className="h-full transition-colors hover:border-primary/50"><CardContent className="p-5"><Icon className="size-6 text-primary" /><p className="mt-7 font-medium">{category.name}</p><p className="mt-2 text-sm leading-6 text-muted-foreground">{category.description}</p><p className="mt-5 text-xs text-primary">{category.jobs} open jobs</p></CardContent></Card></Link> })}</div></section><section className="border-y border-border/70 bg-card/30"><div className="mx-auto max-w-7xl px-5 py-20 sm:px-8"><div className="max-w-2xl"><p className="text-xs font-semibold uppercase tracking-[.18em] text-primary">How it works</p><h2 className="mt-3 text-3xl font-semibold tracking-tight">Less searching. More making.</h2></div><div className="mt-10 grid gap-8 md:grid-cols-3">{[{ icon: Search, title: "Describe the work", text: "Share the shape of the problem and the kind of partner you need." }, { icon: Users, title: "Meet your match", text: "Browse thoughtful proposals and profiles with the context to choose well." }, { icon: CheckCircle2, title: "Make progress", text: "Keep conversations, milestones, and momentum in one clear place." }].map((step, index) => { const Icon = step.icon; return <div key={step.title}><p className="text-sm text-primary">0{index + 1}</p><Icon className="mt-7 size-6" /><h3 className="mt-5 font-medium">{step.title}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">{step.text}</p></div> })}</div></div></section><section className="mx-auto grid max-w-7xl gap-5 px-5 py-20 sm:px-8 md:grid-cols-2"><Card><CardContent className="p-7"><p className="text-xs font-semibold uppercase tracking-[.18em] text-primary">For clients</p><h2 className="mt-4 text-2xl font-semibold">Bring the right expertise into the room.</h2><p className="mt-4 text-sm leading-6 text-muted-foreground">From a first brief to the final handoff, find independent professionals who care about the outcome as much as you do.</p><Button asChild className="mt-7"><Link href="/dashboard/client/jobs/new">Post a job <ArrowRight /></Link></Button></CardContent></Card><Card><CardContent className="p-7"><p className="text-xs font-semibold uppercase tracking-[.18em] text-emerald-300">For freelancers</p><h2 className="mt-4 text-2xl font-semibold">Build a practice around work worth doing.</h2><p className="mt-4 text-sm leading-6 text-muted-foreground">Show your strengths, find better-fit projects, and spend less time managing the work around your work.</p><Button asChild className="mt-7" variant="outline"><Link href="/jobs">Explore jobs <ArrowRight /></Link></Button></CardContent></Card></section><section className="mx-auto max-w-7xl px-5 pb-20 sm:px-8"><div className="rounded-2xl border border-primary/20 bg-primary/5 p-7 sm:p-10"><ShieldCheck className="size-7 text-primary" /><h2 className="mt-5 text-2xl font-semibold">A marketplace built around trust.</h2><p className="mt-3 max-w-2xl text-sm leading-6 text-muted-foreground">Clear profiles, thoughtful conversations, and visible project context help everyone make better decisions before work begins.</p></div></section></main></PublicShell>
}
