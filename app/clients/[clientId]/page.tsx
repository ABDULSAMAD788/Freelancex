import { notFound } from "next/navigation"
import { CheckCircle2, MapPin } from "lucide-react"
import { Card, CardContent } from "@/components/ui/card"
import { PublicShell } from "@/components/layout/public-shell"
import { PageContainer, SectionHeader } from "@/components/shared/page-primitives"
import { JobCard } from "@/components/jobs/job-card"
import { getClient } from "@/lib/mock-data"
export default async function ClientPage({ params }: { params: Promise<{ clientId: string }> }) { const { clientId } = await params; const client = getClient(clientId); if (!client) notFound(); return <PublicShell><PageContainer><div className="rounded-2xl border border-border bg-card p-6 sm:p-8"><div className="flex flex-col gap-5 sm:flex-row sm:items-center"><div className="flex size-20 items-center justify-center rounded-2xl bg-emerald-400/15 text-2xl font-semibold text-emerald-300">{client.initials}</div><div><div className="flex flex-wrap items-center gap-2"><h1 className="text-2xl font-semibold">{client.name}</h1>{client.verified && <span className="inline-flex items-center gap-1 text-xs text-emerald-300"><CheckCircle2 className="size-4" />Verified</span>}</div><p className="mt-2 flex items-center gap-1.5 text-sm text-muted-foreground"><MapPin className="size-4" />{client.location}</p><p className="mt-1 text-sm text-muted-foreground">Member since {client.memberSince}</p></div></div><p className="mt-7 max-w-2xl text-sm leading-7 text-muted-foreground">{client.about}</p></div><div className="mt-6 grid gap-4 sm:grid-cols-3"><Card><CardContent className="p-5"><p className="text-xs text-muted-foreground">Jobs posted</p><p className="mt-2 text-2xl font-semibold">{client.jobsPosted}</p></CardContent></Card><Card><CardContent className="p-5"><p className="text-xs text-muted-foreground">Active jobs</p><p className="mt-2 text-2xl font-semibold">{client.activeJobs}</p></CardContent></Card><Card><CardContent className="p-5"><p className="text-xs text-muted-foreground">Total spent</p><p className="mt-2 text-2xl font-semibold">{client.totalSpent}</p></CardContent></Card></div><div className="mt-12"><SectionHeader title="Jobs posted by this client" description="See what this team is working on now." /><div className="grid gap-4">{client.jobs.map((job) => <JobCard job={job} key={job.id} />)}</div></div></PageContainer></PublicShell> }