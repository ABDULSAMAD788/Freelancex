import Link from "next/link"
import { notFound } from "next/navigation"
import { ArrowLeft, CheckCircle2, Flag, MessageSquare } from "lucide-react"
import { Button } from "@/components/ui/button"
import { Card, CardContent } from "@/components/ui/card"
import { PageContainer, StatusBadge } from "@/components/shared/page-primitives"
import { contracts } from "@/lib/mock-data"
export default async function ContractPage({ params }: { params: Promise<{ contractId: string }> }) { const { contractId } = await params; const contract = contracts.find((item) => item.id === contractId); if (!contract) notFound(); return <PageContainer><Link className="inline-flex items-center gap-2 text-sm text-muted-foreground hover:text-foreground" href="/dashboard/contracts"><ArrowLeft className="size-4" />Back to contracts</Link><div className="mt-8 flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><StatusBadge status={contract.status} /><h1 className="mt-4 text-3xl font-semibold tracking-tight">{contract.title}</h1><p className="mt-2 text-muted-foreground">Working with {contract.otherParty}</p></div><div className="flex gap-2"><Button variant="outline"><MessageSquare />Message</Button><Button variant="destructive"><Flag />Report issue</Button></div></div><div className="mt-8 grid gap-5 sm:grid-cols-3"><Card><CardContent className="p-5"><p className="text-xs text-muted-foreground">Contract value</p><p className="mt-2 text-2xl font-semibold">{contract.budget}</p></CardContent></Card><Card><CardContent className="p-5"><p className="text-xs text-muted-foreground">Started</p><p className="mt-2 text-2xl font-semibold">{contract.startDate}</p></CardContent></Card><Card><CardContent className="p-5"><p className="text-xs text-muted-foreground">Progress</p><p className="mt-2 text-2xl font-semibold">{contract.progress}%</p></CardContent></Card></div><Card className="mt-5"><CardContent className="p-6"><h2 className="font-semibold">Milestones</h2><div className="mt-5 space-y-4">{["Scope and direction", "First review", "Final handoff"].map((milestone, index) => <div className="flex items-center gap-3" key={milestone}><CheckCircle2 className={index === 0 ? "size-5 text-emerald-300" : "size-5 text-muted-foreground"} /><div><p className="text-sm font-medium">{milestone}</p><p className="text-xs text-muted-foreground">{index === 0 ? "Complete" : "Upcoming"}</p></div></div>)}</div></CardContent></Card></PageContainer> }