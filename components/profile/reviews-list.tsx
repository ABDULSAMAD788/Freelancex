import { Star } from "lucide-react"
import { EmptyState } from "@/components/shared/page-primitives"
export function ReviewsList({ hasReviews = true }: { hasReviews?: boolean }) { if (!hasReviews) return <EmptyState title="No reviews yet" description="Reviews will appear here after completed work." />; return <div className="space-y-3">{[{ name: "Jordan Lee", text: "Maya brought structure to a fuzzy brief and made every review feel productive." }, { name: "Sam Rivera", text: "Excellent taste, communication, and follow-through. We shipped ahead of schedule." }].map((review) => <div className="rounded-xl border border-border bg-card p-5" key={review.name}><div className="flex items-center justify-between"><p className="font-medium">{review.name}</p><span className="flex gap-0.5 text-amber-200">{[1, 2, 3, 4, 5].map((star) => <Star className="size-3.5 fill-current" key={star} />)}</span></div><p className="mt-3 text-sm leading-6 text-muted-foreground">{review.text}</p></div>)}</div> }