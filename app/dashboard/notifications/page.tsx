"use client"
import { useState } from "react"
import { Bell, Check } from "lucide-react"
import { Button } from "@/components/ui/button"
import { Card, CardContent } from "@/components/ui/card"
import { PageContainer, PageHeader } from "@/components/shared/page-primitives"
const initialNotifications = [{ id: 1, title: "Maya Chen replied to your message", text: "The latest concept is ready for review.", type: "Message", read: false }, { id: 2, title: "Your proposal was shortlisted", text: "Build a bold brand system for a climate startup.", type: "Proposal update", read: false }, { id: 3, title: "Contract milestone completed", text: "Scope and direction was marked complete.", type: "Contract update", read: true }]
export default function NotificationsPage() { const [items, setItems] = useState(initialNotifications); return <PageContainer><PageHeader eyebrow="Workspace" title="Notifications" description="A quiet record of the moments that need your attention." action={<Button onClick={() => setItems(items.map((item) => ({ ...item, read: true })))} variant="outline"><Check />Mark all read</Button>} /><div className="space-y-3">{items.map((item) => <Card className={item.read ? "opacity-70" : "border-primary/40"} key={item.id}><CardContent className="flex gap-4 p-5"><div className="flex size-9 shrink-0 items-center justify-center rounded-lg bg-primary/10 text-primary"><Bell className="size-4" /></div><div className="flex-1"><div className="flex flex-wrap justify-between gap-2"><p className="font-medium">{item.title}</p><span className="text-xs text-muted-foreground">{item.type}</span></div><p className="mt-1 text-sm text-muted-foreground">{item.text}</p>{!item.read && <Button className="mt-3" onClick={() => setItems(items.map((current) => current.id === item.id ? { ...current, read: true } : current))} size="sm" variant="ghost">Mark as read</Button>}</div></CardContent></Card>)}</div></PageContainer> }