import { PublicShell } from "@/components/layout/public-shell"
import { PageContainer, PageHeader } from "@/components/shared/page-primitives"
export default function HowItWorksPage() { return <PublicShell><PageContainer><PageHeader eyebrow="How it works" title="A clearer path from idea to outcome." /><div className="grid gap-5 md:grid-cols-3">{["Describe the work", "Choose your partner", "Make progress together"].map((title, index) => <div className="rounded-xl border border-border bg-card p-6" key={title}><p className="text-sm text-primary">0{index + 1}</p><h2 className="mt-8 font-semibold">{title}</h2><p className="mt-3 text-sm leading-6 text-muted-foreground">Keep the scope, context, and conversations close enough that everyone can make good decisions.</p></div>)}</div></PageContainer></PublicShell> }